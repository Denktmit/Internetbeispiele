<UserControl x:Class="LoginDemoClient.Views.LoginView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="300" d:DesignWidth="300">
    
    <StackPanel Orientation="Vertical" HorizontalAlignment="Center" VerticalAlignment="Center">
        <TextBox MinWidth="128" Margin="4" Text="{Binding UserName, Mode=TwoWay}"/>
        <TextBox MinWidth="128" Margin="4" Text="{Binding Password, Mode=TwoWay}"/>
        <Button MinWidth="64" Margin="4" Command="{Binding LoginCommand}" Content="Login" HorizontalAlignment="Right"/>
    </StackPanel>
    
</UserControl>
